# Rate limiting for /process endpoint.
# IPs are used in-memory for rate limiting but NOT written to access logs.
# 5 requests/minute per IP — generous for legitimate use, tight for abuse.
limit_req_zone $binary_remote_addr zone=ran_process:10m rate=5r/m;
limit_req_status 429;
